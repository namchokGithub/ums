<?xml version="1.0" encoding="utf-8" ?>
<nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://www.nlog-project.org/schemas/NLog.xsd NLog.xsd"
      autoReload="true"
      throwExceptions="false"
      internalLogLevel="Off" internalLogFile="c:\temp\nlog-internal.log">

  <!-- optional, add some variables https://github.com/nlog/NLog/wiki/Configuration-file#variables -->
  <variable name="myvar" value="myvalue"/>

	<!-- enable asp.net core layout renderers -->
	<extensions>
		<add assembly="NLog.Web.AspNetCore"/>
		<add assembly="NLog.Web"/>
	</extensions>
	
  <targets>
	  <!-- Trace -->
	  <target xsi:type="ColoredConsole" name="logconsole_trace" layout="${date}|${level:uppercase=true}|${message}|${callsite-filename:includeSourcePath=False}:${callsite-linenumber}|${logger}" />	  	  
	  <!-- Debug and Info -->
	  <target xsi:type="Console" name="logconsole_info" layout="${date}|${level:uppercase=true}|${message}${when:when=length('${exception}')>0:Inner=|${exception:format=tostring}}${when:when=length('${aspnet-mvc-action}')>0:Inner=|${aspnet-mvc-action}}:${callsite-linenumber}|${logger}|${aspnet-user-identity}" />	  	  
	   <!--Error--> 
	  <target xsi:type="ColoredConsole" name="logconsole_error" layout="${date}|${level:uppercase=true}|${message}${when:when=length('${exception}')>0:Inner=|${exception:format=tostring}}${when:when=length('${aspnet-mvc-action}')>0:Inner=|${aspnet-mvc-action}}${when:when=length('${aspnet-mvc-controller}')>0:Inner=-${aspnet-mvc-controller}}|${callsite-filename:includeSourcePath=False}:${callsite-linenumber}|${logger}" />
  </targets>

  <rules>
	  <logger name="*" maxlevel="Trace" writeTo="logconsole_trace" />
	  <logger name="*" Levels="Debug, Info" writeTo="logconsole_info" />
	  <!--Skip non-critical Microsoft logs and so log only own logs-->
	  <logger name="Microsoft.*" maxlevel="Info" final="true" />
	  <logger name="*" Levels="Warn, Error, Fatal" writeTo="logconsole_error" />
  </rules>
</nlog>